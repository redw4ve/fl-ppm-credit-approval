[pytest]
# The shipped suite lives in tests/. E_training/_old holds archived workflows whose tests no longer apply,
# so collecting from the repository root must not pick them up.
testpaths = tests
norecursedirs = _old fl-ppm .idea __pycache__ E_training/training_outputs*
